"use client"

import React from 'react'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export function ProxyManagement() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Proxy Management</CardTitle>
        <CardDescription>Configure proxy pools and rotation settings</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Proxy management interface coming soon...</p>
      </CardContent>
    </Card>
  )
}